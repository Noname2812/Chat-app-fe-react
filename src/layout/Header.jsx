import { ModeToggle } from "@/components/mode-toggle";

const Header = () => {
  return (
    <header className="w-screen">
      <div>
        <ModeToggle />
      </div>
    </header>
  );
};

export default Header;
